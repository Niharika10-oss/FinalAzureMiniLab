# FinalAzureMiniLab
fixed namepsace error by setting namespace to default in all manifests and yaml files.
fixed azureSubscriptionEndpoint error by creating Azure resource Manager service connection with manual service principal.
